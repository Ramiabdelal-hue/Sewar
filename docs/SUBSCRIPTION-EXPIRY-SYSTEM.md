# ✅ نظام انتهاء صلاحية الاشتراك

## 📋 الوصف
تم إضافة نظام كامل للتحقق من صلاحية الاشتراك بناءً على الوقت المختار (أسبوعين أو شهر). عند انتهاء الصلاحية، يتم منع المستخدم من الوصول إلى المحتوى.

## 🔧 كيف يعمل النظام

### 1. عند الاشتراك
عندما يشترك المستخدم ويختار المدة:
- **أسبوعين (2 Weeks)**: يتم إضافة 14 يوم من تاريخ الاشتراك
- **شهر واحد (1 Month)**: يتم إضافة شهر من تاريخ الاشتراك

يتم حفظ تاريخ انتهاء الصلاحية (`expiryDate`) في قاعدة البيانات.

### 2. عند الدخول للمحتوى
عند محاولة الدخول لأي صفحة محمية (دروس، امتحانات، فيديوهات):

1. **التحقق التلقائي**: يتم إرسال طلب إلى `/api/check-subscription`
2. **مقارنة التاريخ**: يتم مقارنة تاريخ اليوم مع `expiryDate`
3. **القرار**:
   - ✅ إذا كان الاشتراك ساري: السماح بالدخول
   - ❌ إذا انتهت الصلاحية: عرض رسالة وزر التجديد

### 3. عرض الوقت المتبقي
في جميع صفحات المحتوى، يتم عرض:
- ⏰ الوقت المتبقي للاشتراك (أيام، ساعات، دقائق)
- 📧 البريد الإلكتروني للمشترك
- يتم تحديث العداد كل دقيقة

## 📁 الملفات المعدلة

### 1. API الجديد
**`app/api/check-subscription/route.ts`**
- يتحقق من وجود المستخدم في قاعدة البيانات
- يقارن تاريخ اليوم مع `expiryDate`
- يرجع `expired: true` إذا انتهت الصلاحية

### 2. الصفحات المحدثة
تم إضافة التحقق من الصلاحية في:
- ✅ `app/lessons/page.tsx` - صفحة الدروس النظرية
- ✅ `app/examen/test/page.tsx` - صفحة اختبار الامتحانات
- ✅ `app/praktical/lessons/page.tsx` - صفحة الفيديوهات التدريبية
- ✅ `app/praktical/exam/page.tsx` - صفحة اختبار الخطر

### 3. التحديثات في كل صفحة
```typescript
// 1. إضافة state للتحقق
const [checking, setChecking] = useState(true);
const [isExpired, setIsExpired] = useState(false);

// 2. التحقق عند تحميل الصفحة
useEffect(() => {
  const checkSubscription = async () => {
    const response = await fetch("/api/check-subscription", {
      method: "POST",
      body: JSON.stringify({ email: userEmail })
    });
    const data = await response.json();
    if (data.expired) {
      setIsExpired(true);
    }
  };
  checkSubscription();
}, [userEmail]);

// 3. عرض رسالة إذا انتهت الصلاحية
if (isExpired) {
  return <ExpiredSubscriptionMessage />;
}
```

## 🎨 واجهة المستخدم

### شاشة التحقق
عند تحميل الصفحة:
```
🔄 جاري التحقق من الاشتراك...
```

### شاشة انتهاء الصلاحية
إذا انتهت الصلاحية:
```
🔒 انتهت صلاحية الاشتراك

عذراً، انتهت صلاحية اشتراكك.
يرجى تجديد الاشتراك للوصول إلى المحتوى.

[تجديد الاشتراك]
```

### عرض الوقت المتبقي
في الصفحات النشطة:
```
📧 المشترك: user@example.com
⏰ الوقت المتبقي: 5 أيام 12 ساعة
```

## 🔄 سيناريوهات الاستخدام

### السيناريو 1: اشتراك جديد
1. المستخدم يختار "2 Weeks"
2. يتم حفظ `expiryDate = اليوم + 14 يوم`
3. يمكنه الوصول للمحتوى لمدة 14 يوم
4. بعد 14 يوم، يتم منعه تلقائياً

### السيناريو 2: محاولة الدخول بعد انتهاء الصلاحية
1. المستخدم يحاول الدخول لصفحة الدروس
2. النظام يتحقق من `expiryDate`
3. يجد أن التاريخ قد انتهى
4. يعرض رسالة "انتهت صلاحية الاشتراك"
5. يوفر زر "تجديد الاشتراك"

### السيناريو 3: التجديد
1. المستخدم يضغط "تجديد الاشتراك"
2. يتم فتح نموذج الدفع مع بياناته المحفوظة
3. بعد الدفع، يتم تحديث `expiryDate` بمدة جديدة
4. يمكنه الوصول للمحتوى مرة أخرى

## 🔐 الأمان

### الحماية على مستوى الخادم
- ✅ التحقق يتم في API على الخادم
- ✅ لا يمكن التلاعب بالتاريخ من المتصفح
- ✅ البيانات محفوظة في قاعدة البيانات

### الحماية على مستوى العميل
- ✅ التحقق يتم عند كل تحميل للصفحة
- ✅ لا يمكن تجاوز الفحص بتعطيل JavaScript
- ✅ العداد يتحدث تلقائياً كل دقيقة

## 📊 قاعدة البيانات

### جدول User
```prisma
model User {
  id           Int      @id @default(autoincrement())
  email        String   @unique
  name         String
  password     String
  phone        String?
  category     String
  paymentType  String
  expiryDate   DateTime  // ← تاريخ انتهاء الاشتراك
  status       String
  createdAt    DateTime @default(now())
}
```

### مثال على البيانات
```json
{
  "email": "user@example.com",
  "expiryDate": "2026-03-06T10:30:00.000Z",
  "status": "active"
}
```

## 🧪 الاختبار

### اختبار اشتراك جديد
1. اشترك بمدة "2 Weeks"
2. تحقق من `expiryDate` في قاعدة البيانات
3. يجب أن يكون = اليوم + 14 يوم

### اختبار انتهاء الصلاحية
1. غير `expiryDate` في قاعدة البيانات لتاريخ ماضي
2. حاول الدخول لصفحة الدروس
3. يجب أن ترى رسالة "انتهت صلاحية الاشتراك"

### اختبار العداد
1. اشترك بمدة قصيرة
2. راقب العداد في الصفحة
3. يجب أن يتحدث كل دقيقة

## 🌍 الترجمة

النظام يدعم 3 لغات:
- 🇳🇱 الهولندية (NL)
- 🇫🇷 الفرنسية (FR)
- 🇸🇦 العربية (AR)

جميع الرسائل مترجمة:
- "جاري التحقق من الاشتراك..."
- "انتهت صلاحية الاشتراك"
- "تجديد الاشتراك"
- "الوقت المتبقي"

## ✅ الخلاصة

النظام الآن يعمل بشكل كامل:
1. ✅ يتم حساب تاريخ انتهاء الصلاحية عند الاشتراك
2. ✅ يتم التحقق من الصلاحية عند كل دخول
3. ✅ يتم منع الوصول تلقائياً بعد انتهاء المدة
4. ✅ يتم عرض الوقت المتبقي في جميع الصفحات
5. ✅ يمكن التجديد بسهولة من نفس الصفحة
6. ✅ النظام آمن ومحمي على مستوى الخادم
7. ✅ دعم كامل لـ 3 لغات
