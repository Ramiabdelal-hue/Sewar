# Admin Subscribers Dashboard - إضافة معلومات الدفع

## الميزة الجديدة:

تم إضافة عمودين جديدين في جدول المشتركين:
1. **نوع الدفع** (Payment Type)
2. **المبلغ المدفوع** (Amount)

## الأعمدة الجديدة:

### 1. نوع الدفع:
يعرض طريقة الدفع التي استخدمها المشترك:
- 💵 نقدي (Cash)
- 💳 بطاقة (Card)
- 🏦 Bancontact
- 📱 Payconiq
- 🏛️ تحويل بنكي (Bank Transfer)

### 2. المبلغ المدفوع:
يعرض المبلغ بناءً على نوع الاشتراك:
- دروس نظرية (Theorie): €25
- امتحانات (Examen): €25
- دروس عملية - فيديوهات (Praktijk-lessons): €49
- دروس عملية - إدراك المخاطر (Praktijk-exam): €39

## التصميم:

### نوع الدفع:
```
┌─────────────────────────┐
│ 💳 بطاقة               │  ← badge أخضر مع أيقونة
└─────────────────────────┘
```

### المبلغ:
```
┌─────────────────────────┐
│ 💰 €49                 │  ← badge أصفر مع أيقونة
└─────────────────────────┘
```

## مثال على الجدول:

```
┌────────┬──────────────┬──────────────┬────────┬────────────┬──────────┐
│ الاسم  │ نوع الاشتراك │ الفئة        │ نوع الدفع│ المبلغ    │ الحالة   │
├────────┼──────────────┼──────────────┼────────┼──────────┼──────────┤
│ أحمد   │ دروس نظرية   │ A            │ 💳 بطاقة│ €25      │ نشط ✅   │
│ أحمد   │ امتحانات     │ B            │ 💳 بطاقة│ €25      │ نشط ✅   │
│ أحمد   │ دروس عملية   │ C            │ 💳 بطاقة│ €49      │ نشط ✅   │
└────────┴──────────────┴──────────────┴────────┴──────────┴──────────┘
```

## التغييرات في الكود:

### 1. API (`app/api/admin/subscribers/route.ts`):

```typescript
// إضافة نوع الدفع والمبلغ لكل اشتراك
subscriptionRows.push({
  id: `${user.id}-${sub.id}`,
  userId: user.id,
  name: user.name,
  email: user.email,
  phone: user.phone,
  paymentType: user.paymentType,        // ← جديد
  amount: packagePrices[sub.subscriptionType] || 25,  // ← جديد
  subscriptionType: sub.subscriptionType,
  category: sub.category,
  expiryDate: sub.expiryDate,
  createdAt: sub.createdAt,
  isActive: sub.isActive
});
```

### 2. الواجهة (`app/admin/subscribers/page.tsx`):

#### دالة getPaymentTypeLabel:
```typescript
const getPaymentTypeLabel = (type: string) => {
  const labels: Record<string, string> = {
    'cash': 'نقدي',
    'card': 'بطاقة',
    'bancontact': 'Bancontact',
    'payconiq': 'Payconiq',
    'bank_transfer': 'تحويل بنكي'
  };
  return labels[type] || type;
};
```

#### عرض نوع الدفع:
```tsx
<td className="px-6 py-4 whitespace-nowrap">
  <span className="inline-flex items-center gap-1 px-3 py-1 rounded-full text-xs font-semibold bg-green-100 text-green-800">
    <svg className="w-3 h-3" fill="currentColor" viewBox="0 0 20 20">
      {/* أيقونة بطاقة */}
    </svg>
    {getPaymentTypeLabel(sub.paymentType)}
  </span>
</td>
```

#### عرض المبلغ:
```tsx
<td className="px-6 py-4 whitespace-nowrap">
  <span className="inline-flex items-center gap-1 px-3 py-1 rounded-full text-xs font-bold bg-yellow-100 text-yellow-800">
    <svg className="w-3 h-3" fill="currentColor" viewBox="0 0 20 20">
      {/* أيقونة نقود */}
    </svg>
    €{sub.amount}
  </span>
</td>
```

## الفوائد:

### 1. شفافية مالية:
- رؤية واضحة لطرق الدفع المستخدمة
- معرفة المبلغ المدفوع لكل اشتراك

### 2. تتبع أفضل:
- معرفة طرق الدفع الأكثر استخداماً
- حساب الإيرادات بدقة

### 3. تقارير مالية:
- يمكن تصدير البيانات لإعداد تقارير مالية
- معرفة توزيع الإيرادات حسب نوع الاشتراك

## ترتيب الأعمدة النهائي:

1. الاسم
2. البريد الإلكتروني
3. الهاتف
4. نوع الاشتراك
5. الفئة
6. **نوع الدفع** ← جديد
7. **المبلغ** ← جديد
8. تاريخ الانتهاء
9. تاريخ الاشتراك
10. الحالة

## ملاحظات:

1. نوع الدفع يأتي من جدول User (paymentType)
2. المبلغ يُحسب تلقائياً بناءً على نوع الاشتراك
3. الألوان:
   - نوع الدفع: أخضر (green-100)
   - المبلغ: أصفر (yellow-100)
4. كل عمود له أيقونة SVG مناسبة

## الملفات المعدلة:

1. `app/api/admin/subscribers/route.ts` - إضافة paymentType و amount
2. `app/admin/subscribers/page.tsx` - إضافة العمودين الجديدين ودالة getPaymentTypeLabel
