# تحديث تنسيق الفئات في صفحة الأدمن للامتحانات

## التغيير المطبق

تم تحديث صفحة الأدمن لحفظ أسئلة الامتحانات بتنسيق فئة مختلف عن الدروس النظرية.

## التنسيق الجديد

### قبل التحديث:
```
نوع السؤال: Examen
الفئة المختارة: A
الفئة المحفوظة في DB: A ❌
```

### بعد التحديث:
```
نوع السؤال: Examen
الفئة المختارة: A
الفئة المحفوظة في DB: examA ✅
```

## التفاصيل التقنية

### في دالة `handleAddQuestion`:
```typescript
// تحديد الفئة المناسبة بناءً على نوع السؤال
let categoryToSave = category;
if (questionType === "Examen") {
  // إذا كان نوع السؤال امتحان، احفظ بصيغة examA, examB, examC
  categoryToSave = `exam${category}`;
  console.log("📝 Saving exam question with category:", categoryToSave);
}

formData.append("category", categoryToSave); // استخدام الفئة المعدلة
```

### في دالة `fetchQuestions`:
```typescript
// تحديد الفئة المناسبة بناءً على نوع السؤال
let categoryToFetch = category;
if (questionType === "Examen") {
  categoryToFetch = `exam${category}`;
  console.log("🔍 Fetching exam questions with category:", categoryToFetch);
}

const url = `/api/questions?lesson=${encodeURIComponent(selectedLesson)}&category=${categoryToFetch}&questionType=${questionType}`;
```

## الفئات المختلفة

| نوع السؤال | الفئة المختارة | الفئة المحفوظة |
|------------|----------------|----------------|
| Theori | A | A |
| Theori | B | B |
| Theori | C | C |
| Examen | A | examA |
| Examen | B | examB |
| Examen | C | examC |
| Praktijk | training | training |
| Praktijk | hazard | hazard |

## الفوائد

1. **تمييز واضح**: يمكن التمييز بسهولة بين أسئلة الدروس وأسئلة الامتحانات
2. **استعلامات أسهل**: يمكن جلب أسئلة الامتحانات فقط بسهولة
3. **تنظيم أفضل**: البيانات منظمة بشكل أفضل في قاعدة البيانات
4. **توافق مع نظام الاشتراكات**: يتوافق مع حقل `examCategory` في جدول User

## أمثلة على الاستخدام

### مثال 1: إضافة سؤال امتحان فئة A
```
1. الأدمن يختار: نوع السؤال = "Examen"
2. الأدمن يختار: الفئة = "A"
3. الأدمن يختار: الدرس = "امتحان تجريبي فئة A"
4. الأدمن يكتب السؤال ويضغط "إضافة"
5. النظام يحفظ في DB:
   - category: "examA"
   - questionType: "Examen"
   - lesson: "امتحان تجريبي فئة A"
```

### مثال 2: إضافة سؤال درس نظري فئة B
```
1. الأدمن يختار: نوع السؤال = "Theori"
2. الأدمن يختار: الفئة = "B"
3. الأدمن يختار: الدرس = "قوانين السيارات (B) - مقدمة"
4. الأدمن يكتب السؤال ويضغط "إضافة"
5. النظام يحفظ في DB:
   - category: "B"
   - questionType: "Theori"
   - lesson: "قوانين السيارات (B) - مقدمة"
```

## استعلامات قاعدة البيانات

### جلب جميع أسئلة امتحانات فئة A:
```sql
SELECT * FROM "Question" 
WHERE "category" = 'examA' 
AND "questionType" = 'Examen';
```

### جلب جميع أسئلة دروس نظرية فئة B:
```sql
SELECT * FROM "Question" 
WHERE "category" = 'B' 
AND "questionType" = 'Theori';
```

### جلب جميع أسئلة الامتحانات (كل الفئات):
```sql
SELECT * FROM "Question" 
WHERE "category" IN ('examA', 'examB', 'examC') 
AND "questionType" = 'Examen';
```

## Console Logs للتتبع

عند إضافة سؤال امتحان:
```
📝 Saving exam question with category: examA
✅ Question saved successfully with category: examA
```

عند جلب أسئلة امتحان:
```
🔍 Fetching exam questions with category: examB
✅ Loaded 15 questions for category: examB
```

## التوافق مع الأنظمة الأخرى

### نظام الاشتراكات:
```typescript
// في app/api/subscribe/route.ts
if (body.subscriptionType === "examen") {
  examCategory = `examTest${body.category}`; // examTestA, examTestB, examTestC
}
```

### صفحة الامتحانات:
```typescript
// في app/examen/test/page.tsx
// يمكن الآن جلب الأسئلة بناءً على examCategory
const url = `/api/questions?category=exam${category}&questionType=Examen`;
```

## ملاحظات مهمة

1. **التغيير يؤثر فقط على الامتحانات**: الدروس النظرية والعملية تبقى كما هي
2. **الأسئلة القديمة**: إذا كانت هناك أسئلة قديمة بفئة "A" أو "B" أو "C" ونوع "Examen"، يجب تحديثها يدوياً
3. **Console Logs**: تم إضافة logs للمساعدة في التتبع والتشخيص
4. **التوافق**: النظام متوافق مع نظام الاشتراكات المتعددة الجديد

## تحديث الأسئلة القديمة (إذا لزم الأمر)

إذا كانت هناك أسئلة امتحانات قديمة بتنسيق خاطئ:

```sql
-- تحديث أسئلة امتحانات فئة A
UPDATE "Question" 
SET "category" = 'examA' 
WHERE "category" = 'A' 
AND "questionType" = 'Examen';

-- تحديث أسئلة امتحانات فئة B
UPDATE "Question" 
SET "category" = 'examB' 
WHERE "category" = 'B' 
AND "questionType" = 'Examen';

-- تحديث أسئلة امتحانات فئة C
UPDATE "Question" 
SET "category" = 'examC' 
WHERE "category" = 'C' 
AND "questionType" = 'Examen';
```

## الاختبار

### اختبار 1: إضافة سؤال امتحان
1. سجل الدخول كأدمن
2. اختر "Examen"
3. اختر فئة "A"
4. اختر درس
5. أضف سؤال
6. تحقق من console: يجب أن ترى "📝 Saving exam question with category: examA"
7. تحقق من DB: يجب أن يكون category = "examA"

### اختبار 2: جلب أسئلة امتحان
1. سجل الدخول كأدمن
2. اختر "Examen"
3. اختر فئة "B"
4. اختر درس
5. تحقق من console: يجب أن ترى "🔍 Fetching exam questions with category: examB"
6. يجب أن تظهر الأسئلة المحفوظة بفئة "examB"

### اختبار 3: الدروس النظرية لا تتأثر
1. سجل الدخول كأدمن
2. اختر "Theori"
3. اختر فئة "C"
4. أضف سؤال
5. تحقق من DB: يجب أن يكون category = "C" (وليس "examC")
