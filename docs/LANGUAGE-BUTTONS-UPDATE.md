# ✅ إضافة أزرار اللغات في جميع الصفحات

## 📋 الوصف
تم إضافة أزرار اللغات الثلاث (NL, FR, AR) في جميع الصفحات الرئيسية للموقع.

## 🎨 التصميم

### الموقع
- **Fixed Position**: أعلى يمين الشاشة
- **Z-Index**: 50 (فوق جميع العناصر)
- **Shadow**: ظل لإبراز الأزرار

### الألوان
- 🟠 **NL (الهولندية)**: برتقالي إلى أحمر
- 🔵 **FR (الفرنسية)**: أزرق إلى بنفسجي
- 🟢 **AR (العربية)**: أخضر إلى سماوي

### الحالات
- **نشط**: خلفية ملونة بتدرج + نص أبيض
- **غير نشط**: خلفية بيضاء + نص رمادي + hover effect

## 📁 الصفحات المحدثة

### 1. الصفحة الرئيسية (`app/page.tsx`)
```tsx
<div className="fixed top-4 right-4 z-50 flex gap-2">
  <button onClick={() => setLang("nl")} ...>NL</button>
  <button onClick={() => setLang("fr")} ...>FR</button>
  <button onClick={() => setLang("ar")} ...>AR</button>
</div>
```

### 2. صفحة Theorie (`app/theorie/page.tsx`)
- ✅ أزرار اللغات في أعلى اليمين
- ✅ تعمل مع جميع الفئات (A, B, C)

### 3. صفحة Praktical (`app/praktical/page.tsx`)
- ✅ أزرار اللغات في أعلى اليمين
- ✅ تعمل مع الخيارين (Lessons, Exam)

### 4. صفحة Examen (`app/examen/page.tsx`)
- ✅ أزرار اللغات في أعلى اليمين
- ✅ تعمل في جميع المراحل:
  - اختيار نوع الامتحان
  - اختيار الفئة
  - قائمة الدروس

### 5. صفحة Contact (`app/contact/page.tsx`)
- ✅ كانت موجودة مسبقاً في الـ header

### 6. صفحات أخرى تحتوي على الأزرار
- ✅ `app/lessons/page.tsx` - صفحة الدروس النظرية
- ✅ `app/examen/test/page.tsx` - صفحة اختبار الامتحان
- ✅ `app/praktical/lessons/page.tsx` - صفحة الفيديوهات
- ✅ `app/praktical/exam/page.tsx` - صفحة اختبار الخطر
- ✅ `app/admin/questions/page.tsx` - صفحة الأدمن

## 🎯 الوظيفة

### تغيير اللغة
```typescript
const { lang, setLang } = useLang();

// عند الضغط على زر
onClick={() => setLang("nl")}  // تغيير إلى الهولندية
onClick={() => setLang("fr")}  // تغيير إلى الفرنسية
onClick={() => setLang("ar")}  // تغيير إلى العربية
```

### التأثير
- تتغير جميع النصوص في الصفحة فوراً
- يتم حفظ اللغة في Context
- تبقى اللغة المختارة عند التنقل بين الصفحات

## 📱 Responsive Design

### Desktop
```
[NL] [FR] [AR]  ← أعلى اليمين
```

### Mobile
```
[NL] [FR] [AR]  ← أعلى اليمين (أصغر حجماً)
```

الأزرار تتكيف مع جميع أحجام الشاشات.

## 🎨 الكود المستخدم

```tsx
<div className="fixed top-4 right-4 z-50 flex gap-2">
  <button 
    onClick={() => setLang("nl")} 
    className={`px-4 py-2 rounded-full font-semibold transition shadow-lg ${
      lang === "nl" 
        ? "bg-gradient-to-r from-orange-500 to-red-500 text-white" 
        : "bg-white text-gray-600 hover:bg-gray-100"
    }`}
  >
    NL
  </button>
  <button 
    onClick={() => setLang("fr")} 
    className={`px-4 py-2 rounded-full font-semibold transition shadow-lg ${
      lang === "fr" 
        ? "bg-gradient-to-r from-blue-500 to-purple-500 text-white" 
        : "bg-white text-gray-600 hover:bg-gray-100"
    }`}
  >
    FR
  </button>
  <button 
    onClick={() => setLang("ar")} 
    className={`px-4 py-2 rounded-full font-semibold transition shadow-lg ${
      lang === "ar" 
        ? "bg-gradient-to-r from-green-400 to-cyan-400 text-white" 
        : "bg-white text-gray-600 hover:bg-gray-100"
    }`}
  >
    AR
  </button>
</div>
```

## ✅ الخلاصة

الآن جميع الصفحات تحتوي على أزرار اللغات الثلاث:
1. ✅ الصفحة الرئيسية
2. ✅ صفحة Theorie
3. ✅ صفحة Praktical
4. ✅ صفحة Examen (جميع المراحل)
5. ✅ صفحة Contact
6. ✅ صفحة الدروس
7. ✅ صفحة الاختبار
8. ✅ صفحة الفيديوهات
9. ✅ صفحة اختبار الخطر
10. ✅ صفحة الأدمن

المستخدم يمكنه تغيير اللغة في أي وقت من أي صفحة! 🌍
