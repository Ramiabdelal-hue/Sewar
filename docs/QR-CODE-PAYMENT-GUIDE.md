# دليل الدفع عبر QR Code

## ✅ ما تم تنفيذه

### 1. تثبيت مكتبة QR Code
```bash
npm install qrcode
npm install --save-dev @types/qrcode
```

### 2. تحديث CheckoutForm
- إضافة استيراد `QRCode` من مكتبة qrcode
- إضافة state لحفظ صورة QR Code: `qrCodeImage`
- توليد QR Code حقيقي من رابط الدفع Mollie
- عرض الصورة في Modal

### 3. كيف يعمل النظام

```
1. المستخدم يختار QR Code
2. يضغط "إرسال"
3. ✅ يتم حفظ البيانات بحالة "pending"
4. 🔄 يتم إنشاء دفعة في Mollie (iDEAL method)
5. 📱 يتم توليد QR Code من رابط الدفع
6. 🖼️ يتم عرض QR Code في Modal
7. 📷 المستخدم يمسح الكود بتطبيق البنك
8. 💳 يكمل الدفع في التطبيق
9. 🔔 Mollie يرسل webhook للسيرفر
10. ✅ يتم تفعيل الاشتراك تلقائياً
11. 🎉 يتم إعادة التوجيه للمحتوى
```

## 📱 تجربة المستخدم

### على الموبايل:
1. المستخدم يختار QR Code
2. يضغط "إرسال"
3. يظهر QR Code على الشاشة
4. يفتح تطبيق البنك
5. يختار "Scan QR Code"
6. يمسح الكود
7. يؤكد الدفع
8. يعود للموقع تلقائياً
9. يتم تفعيل الاشتراك

### على الكمبيوتر:
1. المستخدم يختار QR Code
2. يضغط "إرسال"
3. يظهر QR Code على الشاشة
4. يستخدم موبايله لمسح الكود
5. يؤكد الدفع في تطبيق البنك
6. يتم تحديث الصفحة تلقائياً
7. يتم تفعيل الاشتراك

## 🔧 الإعدادات المطلوبة

### 1. Mollie API Key
يجب أن يكون لديك حساب Mollie مفعّل:

```env
MOLLIE_API_KEY=test_xxxxxxxxxxxxxxxxxxxxxxxxxx
NEXT_PUBLIC_BASE_URL=http://localhost:3000
```

### 2. تفعيل iDEAL في Mollie
1. سجل دخول إلى: https://www.mollie.com/dashboard
2. اذهب إلى: **Settings** → **Payment methods**
3. فعّل **iDEAL** (مجاني في وضع الاختبار)

## 🧪 الاختبار

### في وضع الاختبار:

1. افتح الموقع
2. اختر أي اشتراك
3. املأ النموذج
4. اختر **QR Code**
5. اضغط "إرسال"
6. سيظهر QR Code حقيقي
7. امسح الكود بتطبيق البنك (في وضع الاختبار، يمكنك الضغط على الرابط مباشرة)
8. أكمل الدفع
9. سيتم تفعيل الاشتراك تلقائياً

### محاكاة الدفع:

إذا لم يكن لديك تطبيق بنك للاختبار، يمكنك محاكاة webhook:

```bash
curl -X POST http://localhost:3000/api/mollie-webhook \
  -H "Content-Type: application/json" \
  -d '{
    "id": "tr_test123456"
  }'
```

## 🎨 تخصيص QR Code

يمكنك تخصيص شكل QR Code في `CheckoutForm.tsx`:

```typescript
const qrImage = await QRCode.toDataURL(paymentData.checkoutUrl, {
  width: 300,        // حجم الصورة
  margin: 2,         // الهامش حول الكود
  color: {
    dark: '#000000', // لون النقاط
    light: '#FFFFFF' // لون الخلفية
  },
  errorCorrectionLevel: 'M' // مستوى تصحيح الأخطاء: L, M, Q, H
});
```

### أمثلة للتخصيص:

#### QR Code ملون:
```typescript
color: {
  dark: '#FF6B00',  // برتقالي
  light: '#FFF5E6'  // خلفية فاتحة
}
```

#### QR Code كبير:
```typescript
width: 500,
margin: 4
```

#### QR Code عالي الجودة:
```typescript
errorCorrectionLevel: 'H' // يمكن مسحه حتى لو كان 30% منه تالف
```

## 💰 التكاليف

### Mollie - iDEAL (للـ QR Code):
- **€0.29** لكل معاملة
- **بدون رسوم نسبية**
- **بدون رسوم شهرية**

### مقارنة:
- Bancontact: €0.29 + 1.29%
- iDEAL (QR): €0.29 فقط
- **QR Code أرخص للمبالغ الكبيرة!**

### أمثلة:
- اشتراك €25 → رسوم: €0.29 → صافي: €24.71
- اشتراك €50 → رسوم: €0.29 → صافي: €49.71

## 🔒 الأمان

- ✅ QR Code يحتوي على رابط آمن فقط
- ✅ لا يتم تخزين معلومات بطاقات
- ✅ جميع المعاملات مشفرة
- ✅ Mollie معتمد من البنك المركزي الأوروبي
- ✅ QR Code صالح لمدة محدودة (15 دقيقة)

## ⚠️ ملاحظات مهمة

1. **QR Code يعمل مع iDEAL** - وهو نظام دفع هولندي
2. **في بلجيكا** - قد تحتاج لاستخدام Bancontact بدلاً من iDEAL
3. **للاختبار** - استخدم Test API Key
4. **للإنتاج** - استخدم Live API Key وفعّل iDEAL

## 🌍 بدائل للـ QR Code في بلجيكا

### الخيار 1: Payconiq (الأفضل لبلجيكا)
Payconiq هو نظام QR Code بلجيكي:

```typescript
// في create-payment API
method: "payconiq"
```

**مميزات:**
- ✅ شائع جداً في بلجيكا
- ✅ يعمل مع معظم البنوك البلجيكية
- ✅ سهل الاستخدام

**عيوب:**
- ❌ قد يتطلب موافقة إضافية من Mollie

### الخيار 2: Bancontact QR
Bancontact يدعم QR Code أيضاً:

```typescript
method: "bancontact"
// Mollie سيعرض QR Code تلقائياً على الموبايل
```

### الخيار 3: استخدام iDEAL (الحالي)
يعمل في هولندا وبعض الدول الأوروبية

## 🐛 حل المشاكل

### المشكلة: QR Code لا يظهر
**الحل:**
1. تحقق من console.log للأخطاء
2. تأكد من تثبيت مكتبة qrcode
3. تأكد من أن Mollie API Key صحيح

### المشكلة: لا يمكن مسح QR Code
**الحل:**
1. تأكد من أن الكود واضح وكبير
2. جرب زيادة `width` في الإعدادات
3. تأكد من إضاءة الشاشة جيدة

### المشكلة: الدفع لا يعمل بعد المسح
**الحل:**
1. تحقق من أن webhook مفعّل
2. تأكد من أن NEXT_PUBLIC_BASE_URL صحيح
3. راجع logs السيرفر

### المشكلة: "iDEAL not available"
**الحل:**
1. فعّل iDEAL في لوحة تحكم Mollie
2. أو استخدم Bancontact بدلاً منه
3. أو استخدم Payconiq إذا كان متاحاً

## 📋 قائمة التحقق

- [x] تثبيت مكتبة qrcode
- [x] إضافة state لـ QR Code image
- [x] توليد QR Code من رابط الدفع
- [x] عرض QR Code في Modal
- [x] التحقق من حالة الدفع تلقائياً
- [ ] تفعيل iDEAL في Mollie
- [ ] اختبار المسح بتطبيق البنك
- [ ] اختبار على الموبايل
- [ ] اختبار على الكمبيوتر

## 🎯 الخطوات التالية

1. ✅ اختبر QR Code في وضع Test
2. ✅ تأكد من عمل المسح
3. ✅ تأكد من عمل Webhook
4. ✅ جرب على موبايل حقيقي
5. ✅ فعّل iDEAL أو Payconiq للإنتاج
6. ✅ انشر للإنتاج

## 💡 نصائح

1. **استخدم Payconiq في بلجيكا** - أفضل من iDEAL
2. **اجعل QR Code كبيراً** - أسهل للمسح
3. **أضف تعليمات واضحة** - ساعد المستخدمين
4. **اختبر على أجهزة مختلفة** - موبايل وكمبيوتر
5. **راقب معدل النجاح** - قد تحتاج لتحسينات

## 🎉 النتيجة النهائية

- ✅ QR Code حقيقي قابل للمسح
- ✅ يعمل مع تطبيقات البنوك
- ✅ تفعيل تلقائي بعد الدفع
- ✅ تجربة مستخدم سلسة
- ✅ آمن ومحمي
