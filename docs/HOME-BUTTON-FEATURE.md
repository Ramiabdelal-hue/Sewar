# زر العودة للقائمة الرئيسية

## الميزة الجديدة
تم إضافة زر "العودة للقائمة الرئيسية" في Navbar يظهر بعد تسجيل الدخول في جميع الصفحات.

## الموقع
الزر يظهر في **Navbar** على يسار معلومات المستخدم (أو يمين في حالة اللغة العربية).

## التصميم
```
┌─────────────────────────────────────────────────────────────────┐
│  [Logo]  [🏠 القائمة الرئيسية]  [👤 الاسم]  [⏰ الوقت]  [📊 النتائج]  [NL FR AR]  │
└─────────────────────────────────────────────────────────────────┘
```

### الألوان والتصميم:
- **خلفية**: تدرج برتقالي إلى أحمر (من `orange-500` إلى `red-500`)
- **النص**: أبيض
- **الأيقونة**: أيقونة منزل (FaHome)
- **التأثيرات**: 
  - ظل متوسط (shadow-md)
  - تكبير عند التمرير (hover:scale-105)
  - ظل أكبر عند التمرير (hover:shadow-lg)

## الوظيفة

عند الضغط على الزر:
1. **مسح بيانات المستخدم من localStorage**:
   - `userEmail`
   - `userCategory`
   - `userExpiry`

2. **إعادة التوجيه إلى الصفحة الرئيسية**: `/`

3. **النتيجة**: 
   - المستخدم يعود للصفحة الرئيسية
   - Navbar يعود لحالته الأصلية (قبل تسجيل الدخول)
   - يمكن للمستخدم تسجيل الدخول مرة أخرى أو اختيار اشتراك آخر

## الترجمات

| اللغة | النص |
|------|------|
| العربية (AR) | القائمة الرئيسية |
| الهولندية (NL) | Hoofdmenu |
| الفرنسية (FR) | Menu Principal |

## الاستخدام

### السيناريو 1: المستخدم في صفحة الدروس
1. المستخدم يشاهد الدروس
2. يريد العودة للقائمة الرئيسية لاختيار اشتراك آخر
3. يضغط على زر "القائمة الرئيسية"
4. يتم توجيهه للصفحة الرئيسية
5. يمكنه تسجيل الدخول مرة أخرى واختيار اشتراك آخر

### السيناريو 2: المستخدم في صفحة الامتحانات
1. المستخدم ينهي امتحان
2. يريد العودة للقائمة الرئيسية
3. يضغط على زر "القائمة الرئيسية"
4. يتم توجيهه للصفحة الرئيسية

### السيناريو 3: المستخدم لديه اشتراكات متعددة
1. المستخدم مشترك في دروس نظرية + امتحانات
2. دخل إلى الدروس النظرية
3. يريد الانتقال إلى الامتحانات
4. يضغط على زر "القائمة الرئيسية"
5. يتم توجيهه للصفحة الرئيسية
6. يسجل الدخول مرة أخرى
7. يختار "امتحانات" من قائمة الاشتراكات

## الفوائد

1. **سهولة التنقل**: المستخدم يمكنه العودة للقائمة الرئيسية بسهولة
2. **تبديل الاشتراكات**: المستخدمون الذين لديهم اشتراكات متعددة يمكنهم التبديل بينها
3. **تجربة مستخدم أفضل**: واضح ومباشر
4. **متاح في كل صفحة**: الزر يظهر في جميع الصفحات بعد تسجيل الدخول

## الكود

```typescript
<button
  onClick={() => {
    // مسح بيانات المستخدم من localStorage
    localStorage.removeItem("userEmail");
    localStorage.removeItem("userCategory");
    localStorage.removeItem("userExpiry");
    // العودة للصفحة الرئيسية
    window.location.href = "/";
  }}
  className="flex items-center gap-2 bg-gradient-to-r from-orange-500 to-red-500 text-white px-6 py-2 rounded-full shadow-md hover:shadow-lg transition-all hover:scale-105 font-bold"
>
  <FaHome className="text-lg" />
  <span className="text-base">
    {lang === "ar" ? "القائمة الرئيسية" : lang === "nl" ? "Hoofdmenu" : "Menu Principal"}
  </span>
</button>
```

## ملاحظات

1. **الزر يظهر فقط بعد تسجيل الدخول**: إذا لم يكن المستخدم مسجل دخول، لن يظهر الزر
2. **مسح البيانات**: عند الضغط على الزر، يتم مسح جميع بيانات المستخدم من localStorage
3. **إعادة التوجيه**: يتم استخدام `window.location.href` لإعادة تحميل الصفحة بالكامل
4. **التوافق**: يعمل مع نظام الاشتراكات المتعددة الجديد

## الاختبار

### اختبار 1: الزر يظهر بعد تسجيل الدخول
1. افتح الموقع
2. سجل الدخول
3. تحقق من ظهور الزر في Navbar

### اختبار 2: الزر يعمل بشكل صحيح
1. سجل الدخول
2. اضغط على زر "القائمة الرئيسية"
3. تحقق من العودة للصفحة الرئيسية
4. تحقق من مسح بيانات localStorage

### اختبار 3: الترجمات تعمل
1. سجل الدخول
2. غير اللغة إلى NL
3. تحقق من تغير نص الزر إلى "Hoofdmenu"
4. غير اللغة إلى FR
5. تحقق من تغير نص الزر إلى "Menu Principal"

## التحسينات المستقبلية (اختياري)

1. **تأكيد قبل الخروج**: إضافة نافذة تأكيد قبل مسح البيانات
2. **حفظ التقدم**: حفظ موقع المستخدم الحالي قبل الخروج
3. **رسالة ترحيب**: عرض رسالة ترحيب عند العودة للقائمة الرئيسية
4. **إحصائيات**: تتبع عدد مرات استخدام الزر
