# 📋 خريطة بسيطة - ما يجب رفعه على GitHub

## ✅ نعم - ارفعه

```
sa-rijacademie/
│
├── app/                    ✅ كل المجلد
├── components/             ✅ كل المجلد
├── context/                ✅ كل المجلد
├── docs/                   ✅ كل المجلد
├── lib/                    ✅ كل المجلد
├── locales/                ✅ كل المجلد
├── prisma/                 ✅ كل المجلد (schema + migrations + seed)
├── public/                 ✅ كل المجلد (الصور)
│
├── .env.example            ✅ نعم (مثال فقط - آمن)
├── .gitignore              ✅ نعم
├── middleware.ts           ✅ نعم
├── next.config.js          ✅ نعم
├── package.json            ✅ نعم
├── package-lock.json       ✅ نعم
├── README.md               ✅ نعم
├── vercel.json             ✅ نعم
├── tsconfig.json           ✅ نعم
├── tailwind.config.js      ✅ نعم
│
└── جميع ملفات .md         ✅ نعم (الأدلة)
```

---

## ❌ لا - لا ترفعه

```
sa-rijacademie/
│
├── .next/                  ❌ لا (build مؤقت)
├── node_modules/           ❌ لا (ضخم - 500MB)
├── .vercel/                ❌ لا (إعدادات محلية)
├── .env                    ❌ لا (حساس جداً! 🔒)
├── dev.db                  ❌ لا (قاعدة بيانات محلية)
└── *.log                   ❌ لا (ملفات log)
```

---

## 🎯 باختصار

### ارفع:
- ✅ كل المجلدات (app, components, docs, etc.)
- ✅ كل ملفات الإعدادات (package.json, vercel.json, etc.)
- ✅ `.env.example` (مثال فقط)

### لا ترفع:
- ❌ `.env` (كلمات المرور!)
- ❌ `node_modules/` (ضخم)
- ❌ `.next/` (مؤقت)
- ❌ `dev.db` (محلي)

---

## ✅ الأمر الواحد

```bash
cd C:\Users\R_abe\Sewar
git init
git add .
git commit -m "Initial commit - S & A Rijacademie"
git remote add origin https://github.com/YOUR_USERNAME/sa-rijacademie.git
git branch -M main
git push -u origin main
```

**ملاحظة:** `.gitignore` يحمي `.env` تلقائياً! 🔒
